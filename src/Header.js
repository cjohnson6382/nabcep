import React from 'react'

const Header = () => (
	<h3 style={ { background: "#00539f", color: "white", padding: "1em", display: "flex", justifyContent: "center", alignItems: "Center" } } >Header Text</h3>
)

export default Header